Requiment:  First make sure Node is globally installed.

 Run the following commands to install:

    npm install
    node dbserver.js


This will start the api server , it should be available at the following url:

http://localhost:3010
